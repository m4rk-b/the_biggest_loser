<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="css/styles.css">
    <title>The Biggest Loser</title>
</head>
<style>
    
</style>
<body>
    <div class="leader-board border shadow-sm">
        <div class="bg-primary leader-board-title">
            <h3 class="text-light">The Biggest Loser Leader Board</h3>
        </div>

        <% locals.results.forEach(result => { %>
            
        <div class="leader-container bg-light border border-light shadow-sm">
            <div class="img-container">
                <img src="images/user1.png" class="rounded-circle border" alt="">
            </div>
            <div class="info-container">
                <div class="user-container">
                    <p class="fs-6 fw-bold"><%= result.username %></p>
                    <% if (result.weight <= 0) { %>
                        <p class="fs-7 text-muted">Gained <%= Math.abs(result.weight).toFixed(2) %>%</p>
                    <% }else{ %>
                        <p class="fs-7 text-muted">Lost <%= Math.abs(result.weight).toFixed(2) %>%</p>
                    <% } %>
                </div>
                <div class="icon-container">
                    <% if (result.weight <= 0) { %>
                        <i class="bi bi-chevron-compact-up fs-8 fw-bold" style="color: red;"></i>
                    <% }else{ %>
                        <i class="bi bi-chevron-compact-down fs-8 fw-bold" style="color: green;"></i>
                    <% } %>
                </div>
            </div>
            <% if (result.username == locals.trophy) { %>
            <div class="trophy">
                <i class="bi bi-trophy trophy-icon" style="color: #FFD700;"></i>
            </div>
            <% } %>
            
        </div>
        <% }); %>
        
    </div>
    
</body>
</html>